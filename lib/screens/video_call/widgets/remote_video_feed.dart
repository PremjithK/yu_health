import 'package:flutter/material.dart';

class RemoteVideoFeed extends StatelessWidget {
  const RemoteVideoFeed({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
